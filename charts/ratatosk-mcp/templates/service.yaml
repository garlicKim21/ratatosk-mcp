apiVersion: v1
kind: Service
metadata:
  name: {{ include "ratatosk-mcp.fullname" . }}
  labels:
    {{- include "ratatosk-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ratatosk-mcp.selectorLabels" . | nindent 4 }}
